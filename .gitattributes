# Set default behavior in case people don't have core.autocrlf set
* text=input

# Windows scripts should always have windows line-endings on checkout
*.bat text eol=crlf

# Java archives are definitely binary, do not convert line endings
*.jar binary
